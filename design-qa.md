# LINGNAN EDITIONS「岭南辑造」设计与流程验收

验收日期：2026-07-30
运行来源：`http://127.0.0.1:4321`
视觉方向：方案 1「白色美术馆」

## 视觉真相源与对照

- 选定视觉稿：`design-reference.png`
- 首页实现截图：`output/playwright/home-1051x1354.png`
- 同屏对照：`output/playwright/home-comparison.png`
- 原始商品参考：`design-references/stool-*-reference.png`
- 首页以白色美术馆、浅色石灰岩、左侧大面积留白、右侧岭南朱单凳和居中双行字标还原方案 1。
- 实现保留参考图的高留白、黑白红主色、编辑型衬线标题、细线导航和影像优先层级；未复用 Chanel 商标、素材或专属元素。
- 影棚商品图、三色装置图和两张虚构成年米兰模特图均为真实栅格资产；无贴纸、条码、Logo 或水印。模特图在可见文案与替代文本中标注为品牌 AI 概念影像。

## 响应式与视觉检查

使用本地浏览器中的精确 iframe 视口检查三个目标尺寸，媒体查询在 iframe 自身视口计算：

| 视口 | 结果 |
| --- | --- |
| 1440 × 1024 | 无横向溢出；桌面导航可见；首屏标题完整落在视口；全部图片加载成功 |
| 1024 × 768 | 无横向溢出；中型桌面布局生效；首屏标题完整落在视口；全部图片加载成功 |
| 390 × 844 | 无横向溢出；移动菜单取代桌面导航；搜索、会员和购物袋均可达；首屏标题与主图未越界；全部图片加载成功 |

首页视觉稿与本地实现已放入同一个对照画面检查。未发现错误裁切、破图、异常圆角、卡片堆叠、文字撞线或不一致的主色。首页、商品详情、会员、购物袋、结账、订单与物流页面均保持同一编辑型设计语言。

## 浏览器批注复核

- 首页限定作品统一使用 `粤凳 01 / GUANGDONG STOOL N°01`，中文主标题不再出现“广东胶凳”。
- 首屏叙事改为“熟悉的轮廓，在当代生活中重新成形”，强调日常使用与当代空间，不预设购买目的。
- 首页、商品详情、会员中心、购物袋、订单档案和成功页不再出现“虚拟支付”；相关提示只保留在会籍支付与商品结账的支付步骤。
- 品牌主张统一为“岭南为源，辑造当代。 / ROOTED IN LINGNAN. EDITED FOR THE PRESENT.”，可覆盖后续扩展的不同产品类别。
- 面向顾客的界面不再出现 IndexedDB、原子落账、代码目录、缓存或无服务器等实现术语；必要的保存限制改为浏览器层面的通俗说明。
- 订单成功页改为“感谢你的选购”，并以礼宾配送旅程承接后续状态。
- 仓库级界面文案检索确认不再使用“收藏”描述购买、订单或使用目的；既定会籍名称与“三色典藏”具体系列名保持不变。

## 核心流程验收

浏览器中完成了以下完整流程：

1. 首次访问原子创建唯一浏览器身份；刷新与第二标签页保持相同的掩码身份。
2. 使用「岭南礼宾账户」虚拟缴纳 ¥688，会籍更新为 EDITION。
3. 常设瓷象牙款加入 3 件，购物袋按代码目录重新计算为 ¥56,400。
4. 依次完成订单确认、虚构礼宾地址确认和虚拟支付；订单、支付、消费流水、等级、购物袋和物流在一次 IndexedDB 事务中落账。
5. 首单后累计消费更新为 ¥56,400，等级自动更新为 COLLECTOR。
6. 岭南朱限定款从公开“暂时缺货”状态变为可供选购，并完成第二次 ¥38,800 虚拟结账。
7. 订单详情按 0、12、28、45、66、90 秒推进；在“已离开广州”节点刷新页面后继续前进，最终显示“已签收”，未发生状态倒退。
8. 第二标签页读取到相同 COLLECTOR 会籍、空购物袋和两笔历史订单，证明同来源多标签页同步与跨刷新保留正常。

限定和典藏商品在会籍失效、资格不足或超过本机限购时，商品卡、详情按钮、购物袋、结账错误和无障碍名称均只公开“暂时缺货”。仓库级检索未发现“等级不足”或“升级后购买”等泄露文案。

## 自动化验证

- `npm test`：2 个测试文件，19 项测试全部通过。
- 覆盖：身份幂等、¥50,000/¥120,000 边界、受限商品隐藏资格、手工注入高级商品的事务拒绝、年度续会、事务回滚、并发幂等、不可变订单快照和物流单调推进。
- `npm run build`：Vite 生产构建通过。
- 本地主应用标签页与第二业务标签页控制台：0 个 error，0 个 warning。
- 所有商品和商业状态均使用 IndexedDB；未使用 `localStorage` 或内存降级。

## 键盘、无障碍与动态效果

- 提供跳转主要内容链接、可见焦点环、语义化标题、字段集、进度条和有序物流时间轴。
- SPA 路由切换会聚焦主要内容；结账每一步进入、返回和错误回退都会聚焦当前步骤标题。
- 数量按钮包含具体商品名称；移动端保留搜索入口。
- `aria-live` 仅播报物流节点变化，不逐秒播报倒计时。
- `prefers-reduced-motion: reduce` 会关闭视觉过渡，并把虚拟支付等待显著缩短。

## 最终问题分级

- P0：0
- P1：0
- P2：0
- P3：0

本地前端对商品资格的隐藏仅是演示层规则，用户仍可通过开发者工具查看或篡改本地数据；它不是服务端安全控制，也未被描述为真实支付、真实物流或真实防篡改系统。

final result: passed
