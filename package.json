{
  "name": "gd-gochi",
  "version": "0.0.0",
  "private": true,
  "homepage": "https://yanandfish.github.io/gd-gochi/",
  "type": "module",
  "scripts": {
    "catalog:validate": "node scripts/validate-catalog.mjs",
    "dev": "vite",
    "prebuild": "npm run catalog:validate",
    "build": "npm run build:bundle",
    "build:bundle": "vite build",
    "preview": "vite preview",
    "pretest": "npm run catalog:validate",
    "test": "npm run test:unit",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "check:pages": "node scripts/check-pages-build.mjs",
    "check": "npm run catalog:validate && npm run test:unit && npm run build:bundle && npm run check:pages && npm run test:e2e"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@vitejs/plugin-react": "5.0.4",
    "dexie": "^4.4.4",
    "dexie-react-hooks": "^4.4.0",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "react-router-dom": "^7.18.2",
    "vite": "6.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "fake-indexeddb": "^6.2.5",
    "vitest": "^4.1.10"
  }
}
