# 原型实现说明

- 由 Codex 启动本地服务并在当前环境可用的浏览器中打开预览；能直接运行时，不要求用户手动执行启动命令。
- 当视觉来源不清晰或与当前目标不一致时，先使用 Product Design 的 `get-context` 流程。用户给出的持久设计决策需要补充到本文件。
- 从已选定的视觉稿实现时，以 `design-reference.png` 作为布局、组件结构、密度、间距、色彩、字体、可见内容和信息层级的视觉真相源。
- 应用代码统一放在 `src/`。本项目只做本地运行，不保留或调用 Sites 构建与部署配置。
- 代码中的函数、类、类方法和主要组件需要有合适的中文注释；数据对象的每个字段都需要在 JSDoc 类型中说明。
- 本地商业状态只能存入 IndexedDB，禁止使用 `localStorage` 作为身份、会员、购物袋、支付、订单或物流的降级存储。
- 产品资格不足时，对限定和典藏商品只显示“暂时缺货”，包括按钮、提示、购物袋和无障碍文本，不泄露等级要求。
- 商品系列、商品与变体必须通过 `src/catalog/` 下的数据文件接入；每个商品只归入一个主系列，已发布的 `productId` 与 `variantId` 永不改名或复用。
- 新商品自动进入商品总览、所属系列、搜索与新品区域，但不得自动替换首页主视觉或专题大片。
- 新商品运行时资产统一放入 `public/assets/catalog/<product-id>/<variant-id>/`；目录仅保存不含部署前缀的相对资源键。
- 商品 AI 场景必须在目录和顾客界面中明确标注“品牌 AI 概念影像”；普通商品文案不泛用“收藏”，不得编造精确尺寸、材料或工艺事实。
- 根据一句商品说明和样图扩充商品时，优先使用项目级 `$add-lingnan-product` skill，并完成目录、交易、响应式与浏览器验收。
